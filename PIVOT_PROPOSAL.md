# Ocean Treasure Hunt → 피벗 제안 (2026-08-21)

전제: 코드 리팩터링이 아니라 **게임 기획 자체를 바꾼다**. 현재 게임은 "재미가 없다"는 사용자 판단이 출발점.

## 1. 왜 재미가 없는가 — 원인 진단

| # | 원인 | 현재 게임의 증거 |
|---|---|---|
| 1 | **결정이 없다** | 입력은 드래그 하나. 유일한 선택지(Sea Blessings)는 +10% 점수·+0.2s 경고처럼 체감 불가 |
| 2 | **키우는 게 없다** | 런 사이에 남는 건 최고 점수 숫자 하나. 장비·캐릭터·기지 같은 "내 것"이 없음 |
| 3 | **배울 패턴이 없다** | 랜덤 비로 떨어지는 아이템. 패턴 템플릿은 절반이 미구현/버그라 숙련이 안 쌓임 |
| 4 | **실패가 벌이다** | 10분 런 + 생명 3 + 체크포인트 없음. 죽으면 전부 잃고 "다음엔 이렇게 해야지"가 안 생김 |
| 5 | **목표가 운이다** | 점수는 피버(무지개 운)·밈타임·보너스 무한콤보가 지배 |
| 6 | **애착 대상이 없다** | 🤿 이모지 하나. 이름도 성격도 성장도 없음 |

액션 게임의 재미(반사신경·숙련)는 1인 개발 이모지 캔버스로 만들기 가장 어려운 종류다. 반대로 **키우기(성장·선택·환생)**는 1·2·4를 구조적으로 해결하고, 텍스트+숫자+버튼으로 충분해 1인 개발에 유리하다.

## 2. 피벗 옵션

| 옵션 | 내용 | 장점 | 단점 |
|---|---|---|---|
| **A. 잠수부 키우기** (이 리스팅 유지) | 바다 테마 그대로, 장르를 액션→키우기로 교체. v3.0 메이저 업데이트 | 스토어 리스팅·번들·Firebase·Capacitor 재사용, 심사 리스크 최소, 8스테이지 팔레트를 "수심 구간"으로 그대로 전용 | 무료·4+ 어린이 포지션 유지 → 유료 전환 어색 |
| **B. 마라토너 키우기 직행** | 메모리 1순위 콘셉트를 새 앱으로. 이 게임은 그대로 두거나 내림 | 상용화 목표(유료 니치 1위)에 직결 | 이 게임은 "재미없는 채" 방치, 새 리스팅 처음부터 |
| **C. 엔진 선행 → 재스킨** | A로 키우기 엔진(스탯·업그레이드·환생·이벤트 카드·오프라인 수익·저장)을 데이터 드리븐으로 만들어 검증 후, 같은 엔진을 마라토너 테마로 유료 출시 | 기존 리스팅에서 루프를 공짜로 검증, 엔진 70% 재사용 | 테마=콘텐츠라 콘텐츠 작업은 두 번 |

**추천: C (A를 1차로, B를 2차로).** 이유: 키우기 게임의 코드는 대부분 테마 무관(틱 루프·성장 곡선·환생 배수·이벤트 카드 시스템·저장)이고, 이미 출시된 무료 리스팅은 "재미 검증 샌드박스"로 이상적이다. 여기서 루프가 재미있다고 확인되면 마라토너는 테마 데이터만 갈아끼운다.

## 3. 추천안 1장 콘셉트 — 「잠수부 키우기」 (가제)

제목 후보: 「보물 못 찾으면 또 환생함」(벤치마크 패턴, 밈성) / 「잠수부 키우기: 심해 1만 미터」(검색형) / 「또 잠수함: 잠수부 키우기」

### 핵심 루프 (1세션 = 1다이빙, 30~60초)
1. **잠수** — 잠수부가 자동으로 내려간다. 수심이 깊어질수록 보물 가치↑, 산소 소모↑, 위험 이벤트↑.
2. **결정** — 핵심 재미는 **push-your-luck**: 화면 중앙에 「더 내려간다 / 올라간다」 버튼. 산소 게이지를 보며 "한 구간만 더?"를 판단. 중간중간 이벤트 카드(상어 출현: 도망/숨기/작살, 난파선 발견: 탐색/통과)로 선택 2~3회.
3. **귀환** — 올라오면 보물 정산. **산소 고갈 = 환생**(구조돼 병원행, 보물 전부 잃음, 대신 "경험 진주" 영구 보너스).
4. **성장** — 골드로 장비 업그레이드: 산소통(시간) / 오리발(하강 속도) / 랜턴(깊은 수심 보물 발견율) / 작살(위험 이벤트 승률) / 그물(보물 수량) / 잠수정(오프라인 자동 수익).
5. **방치** — 앱을 꺼도 잠수정이 자동 채굴(최대 8시간 캡). 돌아오면 "잠수정이 ○○골드를 건져왔어요".

### 진행 축 = 기존 8스테이지 팔레트를 수심 구간으로 전용
햇살 얕은 바다(0~50m) → 산호 카니발 → 켈프 미로 → 난파선 → 심해 발광층 → 열수 분출구 → 빙하 해류 → **아틀란티스**(밈 리프 교체). 구간마다 새 보물·생물 도감 해금. 기존 `STAGES`의 그라디언트·floor·weed 색상, 보물 8종·장애물·이벤트 이름을 그대로 데이터로 승격.

### 환생 = 벌이 아니라 기능
- 환생 n회차 타이틀 ("3번 죽은 잠수부"), 환생마다 시작 보너스 배수·새 이벤트 카드 해금.
- 환생 횟수 자체가 자랑거리(공유 카드 이미지 생성 → 인스타/카톡 바이럴).

### 장기 목표
- **최대 수심 리더보드** (Firebase 그대로, score→depth로 의미 변경).
- 도감: 보물 30종·해양생물 20종·장비 스킨.
- 7~12세도 2030도 같은 루프("한 칸 더?")에 반응하므로 4+ 유지 가능.

### 기존 액션 파트의 처분
드래그 수집 미니게임은 삭제하거나, "피버 다이빙" 10초 보너스로만 축소. 기본은 삭제 권장(유지 비용·120Hz 문제 모두 사라짐).

### 기술 방향
- UI는 **DOM 기반**(텍스트·숫자·버튼·게이지)이 맞다. 캔버스는 배경 연출(기존 파도·버블·그라디언트)만 유지.
- 사실상 새 코드. 가져갈 것: 팔레트/아이템 데이터, Web Audio 신스, Firebase 닉네임·리더보드·오프라인 큐, Capacitor 셸, i18n 테이블, privacy.html.
- 엔진은 JSON 데이터 드리븐(`stats`, `upgrades`, `zones`, `events`, `prestige`)으로 — 마라토너 재스킨 전제.
- 단일 HTML 유지 가능. Vite 도입 여부는 2차.

### 일정 감각
- 1주차: DOM 프로토타입(숫자 밸런스 + 환생 루프 + 이벤트 카드 10장). 재미 판정은 여기서.
- 2주차: 수심 8구간 콘텐츠·도감·오프라인 수익·리더보드 연결·연출.
- 3주차: v3.0 심사 제출. 이후 루프가 검증되면 마라토너 테마 데이터 작업 시작.

## 4. 사용자 결정 필요
1. **리스팅**: 이 앱(무료·4+)을 v3.0으로 갈아끼울지(추천), 새 앱으로 낼지.
2. **액션 파트**: 완전 삭제(추천) vs 보너스 미니게임으로 잔존.
3. **마라토너와의 관계**: 엔진 선행 제작(추천)으로 볼지, 이 게임은 독립 프로젝트로 볼지.
4. **제목 톤**: 밈형(「보물 못 찾으면 또 환생함」) vs 검색형(「잠수부 키우기」). 어린이 리스팅이면 검색형이 안전.

합의되면 다음 산출물: 1장 GDD 확정 → 수치 밸런스 시트(성장 곡선·산소·수익) → DOM 프로토타입.
