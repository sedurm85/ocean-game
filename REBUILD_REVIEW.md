# Ocean Treasure Hunt — 신규 리빌딩 검토 (2026-08-21)

## 0. 결론 먼저

**"리빌드"가 아니라 "모듈화 + 시간 단위 통일 + 백엔드 규칙 정비 + 기획 다이어트"가 필요한 상태.**

- 게임 로직은 CSS/HTML/i18n/데이터 테이블을 빼면 약 1,100줄. Canvas 2D로 충분한 규모라 새 엔진(Phaser/Pixi)으로 옮겨도 얻는 것보다 게임 필 재튜닝·버그 재발 리스크가 큼.
- 새 스택으로 가도 Firebase 보안·120Hz 타이밍·App Store UGC 대응은 똑같이 해야 하므로 리라이트의 한계 효용이 낮음.
- 반면 **현재 상태로 방치는 불가**: 출시된 앱(v2.0)에 오프라인 첫 실행 크래시, 120Hz 밸런스 붕괴, 리더보드 XSS, 점수 조작 무방비가 살아 있음.

| 선택지 | 공수 | 판단 |
|---|---|---|
| (a) 제자리 리팩터 (Vite + ES modules, 선택적 TS) | 3~5일 | **권장** |
| (b) 새 스택 리라이트 | 1.5~3주 | 비권장 (기획 자체를 갈아엎을 때만) |
| (c) 유지 | 0 | 불가 — 아래 P0 4건 때문 |

## 1. 현재 상태 스냅샷

| 항목 | 값 |
|---|---|
| 코드 | `index.html` 단일 파일 1,960줄 / 99KB, 마지막 커밋 2026-02-26 |
| 웹 배포 | https://sedurm85.github.io/ocean-game/ — main과 동기화, 라이브 |
| App Store | **출시됨** (id6759148710). 2026-02-22 출시, v2.0 02-26 업데이트, 무료, KR 평점 5.0(1건) |
| iOS 프로젝트 | `~/ocean-game-app` Capacitor 8, MARKETING_VERSION 2.0 |
| 미커밋 | `DEVLOG.md` 1행에 `dddd` 오타 (실수 편집, 되돌리면 됨) |
| 레포 잡동사니 | `index.html.v1.bak` 61KB 커밋돼 있음 (git 이력이 있으니 삭제 가능) |

## 2. 즉시 수정 (P0 — 리빌드 여부와 무관하게 출시본 패치 대상)

1. **Firebase CDN 로드 실패 = 검은 화면** (`index.html:9-11`, `:150`). `<script src=gstatic>`이 안 떠 있으면 `firebase.initializeApp`에서 ReferenceError → 스크립트 블록 전체 중단 → `init()`(1957행) 미실행. "오프라인 모드"는 SDK가 이미 떠 있는 경우에만 동작. 비행기 모드에서 앱 첫 실행 시 재현. Jua 폰트도 Google Fonts `@import` 의존.
   - 조치: SDK·폰트를 번들에 포함, 초기화를 try/guard로 감싸 실패해도 플레이 가능하게.
2. **프레임 기반 타이밍 잔존 → 120Hz(ProMotion)에서 2배속**. a7e97ea는 아이템 낙하(`:1534`)만 px/s로 바꿈. 아직 프레임 단위: 다이버 lerp `:1524`(0.15/f), 상어 `:1543`, 얼음 `:1544`, 자석 `:1549`, 히트스톱 `:1399`, 파티클·플로팅텍스트 `:1409/:1415`, 쉐이크 감쇠 `:1420`, 버블 스폰 `:1355`.
   - 조치: 전부 dt 기반. 60fps 등가 환산표는 §6.
3. **리더보드 XSS** (`:645` `innerHTML`에 닉네임 삽입). DB에 직접 쓴 닉네임은 클라이언트 정규식으로 못 막음 → `textContent`.
4. **점수 조작 무방비**. 클라이언트가 `players/{uid}.update({highScore})`(`:662`) 직접 쓰기. DEVLOG의 제안 규칙을 적용해도 자기 노드에 임의 숫자 가능하고, 그 규칙은 `players` 부모 쿼리(`:633`)를 막아 리더보드가 깨짐.
   - 조치: 레포에 `database.rules.json` 추가 — `players` 레벨 `.read` + `.indexOn:["highScore"]`, `.validate`(숫자·상한·단조증가·닉네임 정규식·서버 타임스탬프), App Check. 근본 방어는 Cloud Function `submitScore`.
   - **지금 콘솔 규칙이 "테스트 모드"인지 확인 필요** (DEVLOG는 30일 한정이라 적혀 있고 6개월 지남).

추가 운영 리스크: 익명 auth는 재설치마다 새 uid → 닉네임 영구 점유·고아 노드 누적. 4+ 등급에서 UGC 닉네임 공개 노출인데 비속어 필터·신고가 없음(Guideline 1.2). 계정/데이터 삭제 경로 없음(5.1.1(v)).

## 3. 코드 구조 진단 (리팩터 근거)

- 전역 `let` 약 57개(`:353-424`), 상태 기계는 `gameState === '...'` 문자열 비교 약 35곳에 산재.
- God function: `update()` 122줄, `updateBossEvent()` 112줄, `draw()` 107줄, `drawBossEvent()` 86줄(update와 **평행 switch** — 보스 하나 추가 시 3곳 수정), `collectItem()` 83줄.
- 중복: 아이템 리터럴 7벌, localStorage fallback 5벌, 파워업→텍스트 매핑 2벌.
- 진실 소스 분산: 축복 수치가 TEXTS 문자열(ko/en)과 코드 3곳에 따로, `ITEM_TYPES.dur`은 아무도 안 읽는 죽은 데이터(실제는 `activatePowerup` 하드코딩).
- 빈 catch `:453`, `:739`; `JSON.parse(localStorage…)`가 try 없이 3곳 → 저장소 깨지면 시작 화면 크래시.
- 로직 버그: 게임오버 프레임에 남은 보물이 계속 수집돼 최종 점수 동기화 후 score 변동(`:1214` vs `:1532-1564`); `setTimeout` 500ms가 재시작 후 새 런에 하트버블 주입(`:1288`); 피격 무적 시간 없음 → 장애물 겹치면 더블히트.
- 성능: HUD를 매 프레임 DOM 쓰기(`:1363-1394`), 다이버 `shadowBlur` 10~25 매 프레임(`:1638`), 이모지 `fillText` 매 프레임(오프스크린 캐시 권장), DPR 상한 없음(3x 기기 백버퍼 과다), 그라디언트 매 프레임 재생성.
- **잘 되어 있어 그대로 들고 갈 것**: `STAGES`(264-313), `ITEM_TYPES`(316-340), `SEA_BLESSINGS`(343-350), `TEXTS`(155-259), 오디오 신스 전체(433-514), 레인/웨이브 예산/패턴 3종, 콤보·별점·미션 판정 규칙, 보스 타이밍 상수, Firebase 오프라인 큐 개념.

## 4. 기획(V2 GDD) 대비 구현 갭

구현 완료율 체감: 뼈대(8스테이지·별·축복·보너스·보스 상태머신) ✅, 디테일·리텐션 ❌.

**구조적 결함**
- **스테이지 8 미션 "두쫀쿠 포획" 달성 불가**: 두쫀쿠는 런당 1회, 스테이지 3 진입 시 무조건 스폰 예약(`:786`), 스폰 즉시 플래그(`:1050`) → 8스테이지엔 절대 안 나옴. 3에서 잡아도 미션 카운터엔 미기록.
- 보스 8종 중 4종 판정 결함: Crab Parade 충돌 판정 없음(장식), Eel Beam/Vent는 종료 순간 1회만 판정, Kraken은 경고 없이 활성화=피격.
- 보너스 라운드 중 콤보·파워업 타이머 정지(`:1449` 조기 return) → 10초 무한 콤보.
- GapWall 패턴이 항상 왼쪽 정렬 뭉치(`:969-978`), RainSlow는 안 느림.
- Blessing ⑥(경고 +0.2s) 플래그를 읽는 코드 0곳.
- HUD에 미션 진행도 없음("조개 12/25") → 미션 시스템이 독자에게 보이지 않음.

**미구현**: speedRamp 열 전체, ZigZag/Tunnel/Switchback 패턴, busy 웨이브, 수집 펀치 스케일, 피치 랜덤 SFX, 피격 무적/블링크, BGM 로우패스, 팔레트 액센트 8종 전부, 지도 3장 +150, 보너스 +30%, 스킨 4종, 업적 8종, 데일리 챌린지, 시작 화면 별 표시.

**기획 자체의 문제 (V3 GDD에서 손볼 것)**
1. 런 길이 ≈ 10분, 체크포인트 없음. 별은 저장되는데 스테이지 선택이 없어 후반 콘텐츠(보스 5~8, 신규 장애물)를 대부분의 아이가 못 봄. 7~12세 집중 시간(2~4분)과 불일치.
2. 생명 3 + 8분 + 무적 없음 + 장애물 36% + 230px/s → "불공평한 죽음" 구조. 스테이지 7 미션 "생명 2개 유지"는 난이도 고백.
3. 미션 편차 극단: 5스테이지 "피버 1회"는 자동 달성, 8스테이지는 달성 불가, 2스테이지 "x3 콤보 3회"는 정의 모호.
4. 축복 6종 중 3종(+10% 점수, +0.2s 경고, +0.5s 유예)은 아이가 체감 불가. 13px 텍스트 카드 3장 선택 UX.
5. 점수 리더보드(피버·밈타임·보너스 콤보 지배) vs 스테이지 진행 — 목표 축 충돌.
6. "두쫀쿠/밈 리프" 스테이지: 영어권 무의미, 🤡 이모지와 의미 불일치, 유행 유통기한, 실제 이미지 넣는 순간 5.2(IP) 리스크. 한 스테이지 정체성 전체를 밈에 건 과투자.
7. 장애물 O5/O6/O7은 낙하 아이템으로 만들면 해파리 리스킨. 보스 전용 해저드와 낙하 장애물을 분리했어야 함.
8. 24성 스킨·데일리 챌린지는 스테이지 선택 인프라 없이는 도달 불가 보상.

## 5. 권장 로드맵

### Phase 0 — 핫픽스 릴리스 (v2.1, 1~2일, 구조 안 건드림)
- P0 1~4 (CDN 번들, dt 통일, XSS, rules.json + validate)
- 피격 무적 0.8s + 블링크, 보너스 라운드 타이머 버그, 게임오버 후 수집 차단
- 닉네임 비속어 필터 + 리더보드 숨김/신고 최소 대응
- `DEVLOG.md` 오타 복구, `index.html.v1.bak` 삭제

### Phase 1 — 모듈화 (3~5일, "리빌딩"의 실체)
1. Vite 도입, Firebase SDK·폰트 로컬 번들, 초기화 guard
2. 데이터 추출: `stages.js` / `items.js`(dur을 유일한 진실로) / `blessings.js`(수치+템플릿 문자열) / `i18n.js` / `audio.js`
3. 시스템 분리: `state.js`(단일 상태 객체 + 전이 함수), `spawn.js`(패턴 = 레인 + 역할 슬롯), `boss/*.js`(이벤트별 `{init,update,draw}` 테이블 → 평행 switch 제거, "경고 ≥0.8s → 활성 구간 내내 판정 → 쿨다운" 공통 규칙), `render.js`(이모지 스프라이트 캐시, 그라디언트 캐시, DPR≤2), `hud.js`(diff 기반), `firebase.js`, `input.js`(pointer events)
4. Capacitor `www/`를 Vite 빌드 산출물로 교체

### Phase 2 — V3 기획 다이어트 (GDD 개정 후 구현)
- **Keep**: STAGES 테이블, 9레인 스포너, 별 3단계+저장, 보스 상태머신(개수 3~4로), 피격 주스 3종, 콤보 테이블, Heart Bubble, 보너스 라운드, 축복 프레임(Shield/Magnet/Slow 3종만)
- **Cut**: 밈 스테이지 정체성(두쫀쿠는 순수 카메오로), O5/O6/O7 낙하 버전, 다중 웨이브 패턴 3종, speedRamp, 축복 ④⑤⑥, 지도 +150, 데일리 챌린지, 60s×8 → **40~45s×6 또는 60s×5**
- **Add**: 스테이지 선택/이어하기(저장 데이터 이미 있음 — 가장 싼 리텐션 개선), 미션 진행도 HUD, 첫 20초 장애물 0% 튜토리얼, 연속 게임오버 시 숨은 난이도 보정, 스테이지당 파랄랙스 장식 1개(액센트 8종 대체), 스킨 2종·업적 4종

### 결정 필요 사항 (사용자)
- 이 게임의 포지션: 어린이 무료 캐주얼 유지 vs. 메모리의 "유료 니치 게임 파이프라인" 연습작. 후자라면 Phase 1까지만 하고 Phase 2 기획은 신작(마라토너 키우기)에 투입하는 게 효율적.
- Firebase 콘솔 현재 규칙 확인 (테스트 모드 여부) — 접근 권한이 사용자에게만 있음.

## 6. dt 환산표 (60fps 기준, 필 보존용)

- 다이버 lerp 0.15/f → `x += dx * (1 - exp(-9.75*dt))`, DASH 0.21 → 14.1/s, 자석 0.05 → 3.1/s
- 파티클 ±5px/f → ±300px/s, 중력 0.15px/f² → 540px/s², 수명 30f → 0.5s (축하 90f/120f → 1.5s/2s)
- 플로팅 텍스트 1px/f → 60px/s, 수명 60f → 1s
- 쉐이크 감쇠 `*=0.88/f` → `*= pow(0.88, dt*60)`; 히트스톱 6f → 0.1s, 9f → 0.15s
- 아이템 흔들림 1.2px/f → 72px/s, 상어 2.5 → 150px/s, 얼음 1.5 → 90px/s, 보스 드리프트 ±2.5 → ±150px/s
- 버블 0.5~1.5px/f → 30~90px/s, 스폰 5%/f → 초당 3개
